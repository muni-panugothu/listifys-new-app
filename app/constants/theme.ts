/**
 * @deprecated Prefer `useTheme().colors` from `@/providers/theme-provider`.
 * Kept as the light-mode fallback for any legacy call sites during migration.
 */
export const APP_SCREEN_BG = "#F6F7F8";

/** Brand primary — identical in light and dark. */
export const APP_BRAND_PRIMARY = "#27BB97";
