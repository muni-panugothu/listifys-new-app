import { EventDetailImmersiveScreen } from "@/features/events/screens/event-detail-immersive-screen";

/** Immersive event details — hero parallax, horizontal event pager, sticky CTA. */
export function EventDetailScreen() {
  return <EventDetailImmersiveScreen />;
}
